{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "TokenKey": "This is the key used to sign the JWT token This is the key used to sign the JWT token This is the key used to sign the JWT token",
  "AllowedHosts": "*"
}
